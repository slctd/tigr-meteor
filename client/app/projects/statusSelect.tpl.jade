button.btn.btn-white.dropdown-toggle(data-toggle="dropdown" type="button")
  if status
    span(class='{{statusClass}}') {{statusLabel}}
  else
    | Status
  span.caret
ul.dropdown-menu
  if statusItems
    each statusName in statusItems
      +statusSelectItem statusName=statusName